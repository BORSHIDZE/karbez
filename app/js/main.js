$(function name(params) {

  $('.review__slider').slick({
    autoplay: true,
    autoplaySpeed: 2000,
    fade: true,
  });

  $('.portfolio__slider').slick({
    arrows: false,
    dots: false,
  });

})  